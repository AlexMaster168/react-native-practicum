import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    settingButton: {
        position: 'absolute',
        top: 12.5,
        right: 55,
    },
    modalContainer: {
        justifyContent: 'flex-end',
        alignItems: 'center',
        margin: 0,
    },
    modalContent: {
        borderRadius: 3,
        backgroundColor: '#fff',
        padding: 15
    },
})
